import { Routes } from '@angular/router';
import { WorkProcessComponent } from './features/work-process/work-process.component';

export const routes: Routes = [
  { path: '', component: WorkProcessComponent }
];
